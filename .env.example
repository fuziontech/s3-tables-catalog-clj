# AWS Configuration
AWS_REGION=us-west-2
WAREHOUSE_BUCKET=s3://your-bucket/warehouse

# Optional: AWS credentials (if not using ~/.aws)
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key